name: Publish to crates.io

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    name: Validate crate release
    runs-on: ubuntu-24.04
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Validate release metadata
        run: python scripts/release_metadata.py

      - name: Run packaging helper tests
        run: python -m unittest discover -s tests -p "test_*.py"

      - name: Validate workflow pins
        run: python scripts/check_workflows.py

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Check clippy
        run: cargo clippy --all-targets --all-features --locked -- -D warnings

      - name: Check repository diff
        run: git diff --check

      - name: Run Rust tests
        run: cargo test --all-targets --locked

      - name: Run layer-analysis benchmark
        run: cargo bench --locked --bench layer_analysis

  package:
    name: Validate Cargo package
    needs: validate
    runs-on: ubuntu-24.04
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Validate package
        run: cargo publish --locked --dry-run
      - name: Inspect packaged crate contents
        run: cargo package --locked --list

  publish:
    name: Publish crate
    if: ${{ github.event_name == 'push' && github.ref_type == 'tag' }}
    needs: package
    runs-on: ubuntu-24.04
    environment:
      name: crates-io-release
      url: https://crates.io/crates/bbtidy
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Validate release metadata
        run: python scripts/release_metadata.py

      - name: Authenticate with crates.io
        id: auth
        uses: rust-lang/crates-io-auth-action@c6f97d42243bad5fab37ca0427f495c86d5b1a18 # v1.0.5

      - name: Publish crate
        run: cargo publish --locked
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}
